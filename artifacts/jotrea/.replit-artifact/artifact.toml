kind = "web"
previewPath = "/"
title = "Jotrea"
version = "1.0.0"
id = "artifacts/jotrea"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24795

[services.development]
run = "pnpm --filter @workspace/jotrea run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jotrea", "run", "build" ]
publicDir = "artifacts/jotrea/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24795"
BASE_PATH = "/"
